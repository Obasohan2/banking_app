{
  "name": "banking-app",
  "version": "1.0.0",
  "private": true,
  "description": "Node placeholder for Heroku buildpack detection",
  "engines": {
    "node": "20.x"   // or "18.x", "22.x" — pick one you tested locally
  },
  "scripts": {
    "build": "echo 'No frontend build step required'",
    "start": "node index.js"   // ← CHANGE THIS to your actual entry file
  }
}